# This exercise will require the application to ask the user for a name and the app will count the letters in the name and print to the screen.

name = input("What is your name? ")
print("Welcome to the app " + name + "!!")
print(name + ", your name has " + str(len(name)) + " letters in it!")
